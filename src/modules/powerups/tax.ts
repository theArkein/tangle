import type { PowerUpDefinition } from "./types";

export const TAX: PowerUpDefinition = {
  id: "tax",
  name: "Tax",
  category: "offensive",
  rarity: "common",
  description: "Reduce opponent's current round score by 4 points (minimum 0).",
};
