{
  "name": "tangle-worker",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "dev:lan": "npx concurrently -n worker,frontend -c blue,green \"lsof -ti:8787 | xargs kill -9 2>/dev/null; wrangler dev --ip 0.0.0.0\" \"lsof -ti:3000 | xargs kill -9 2>/dev/null; LAN_IP=192.168.0.104 npm run dev --prefix frontend -- -H 0.0.0.0\"",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "test:watch": "vitest",
    "type-check": "tsc --noEmit",
    "db:migrate": "wrangler d1 migrations apply tangle-db --local",
    "db:seed-gen": "tsx scripts/load-dictionary.ts"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.8.0",
    "@cloudflare/workers-types": "^4.0.0",
    "tsx": "^4.22.4",
    "typescript": "^5.8.3",
    "vitest": "^3.2.2",
    "wrangler": "^4.98.0"
  }
}
